N := 256
c := 5e-9
d := 20e-9
setgridsize(N, N, 1)	
setcellsize(c, c, d)
// ${unique_word}

Msat  = 800e3
Aex   = 13e-12
alpha = 0.02
bstat := 10.6e-4

m = uniform(1, .1, 0)
relax()
save(m)    // relaxed state

autosave(m, 200e-12)
maxdt = 1e-12

B_ext = vector(bstat, 0, 0)
run(1e-9)
